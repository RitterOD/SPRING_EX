package org.maslov.antrl4demo.parser;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SQLQueryParserTest {

 /*   @Test
    void simpleSelect() {
        String query = "SELECT id, login, password FROM accounts";
        SQLQueryParser parser = new SQLQueryParser();
        SQLQuery rv = parser.parseQuery(query);
        assertEquals("accounts", rv.getTableName());
        assertEquals(3, rv.getColumns().size());
        assertEquals("id", rv.getColumns().get(0));
        assertEquals("login", rv.getColumns().get(1));
        assertEquals("password", rv.getColumns().get(2));
    }
*/
}